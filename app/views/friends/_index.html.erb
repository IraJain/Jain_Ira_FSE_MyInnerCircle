<table>


  <% @friends.each do |friend| %>
      <tr>
        <td><%=User.find(friend.friend_id).first_name + " " +
                       User.find(friend.friend_id).last_name %> <br />

          <%= "(" + User.find(friend.friend_id).email + ")"%></td>

        <td><%= link_to 'Delete', friend, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
</table>



<!--


  <!% @friends.each do |friend| %>

      <div class="container-fluid" >
        <div class="row-fluid">

          <div class="span12">

            <div class ="span10">
                  <!%=User.find(friend.friend_id).first_name + " " +
                                     User.find(friend.friend_id).last_name %>
            </div>

            <div class ="span2">
            <!%= link_to 'Delete', friend, method: :delete, data: { confirm: 'Are you sure?' } %>
             </div>

          <!%= "(" + User.find(friend.friend_id).email + ")"%>
       </div>



     </div>
            </div>



  <!% end %>-->
